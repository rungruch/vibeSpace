export enum ElearningVisibility {
    General = "public",
    Password = "private",
    InviteOnly = "unlisted"
}

export enum ElearningMediaType {
    Video = "video",
    Editor = "editor",
    PDF = "pdf",
    Quiz = "quiz"
}